import 'package:flutter/material.dart';

class MyTripsScreen extends StatelessWidget {
    static const routeName = '/myTrips';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
       body: Container(
      alignment: Alignment.center,
      child: Text("Here my Trips screen!!!!"),
    ));
  }
}
